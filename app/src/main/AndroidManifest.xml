<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a14860.googletest07">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA1GNXSe8nqsh7GtAwTtMWlffs7GTLXOaI" />

        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BaseActivity"/>
        <activity android:name=".labeling.building1"/>
        <activity android:name=".labeling.MarkerActivity"/>
        <activity android:name=".location.MyLocationDemoActivity"/>
        <activity android:name=".photoView"/>
        <activity android:name=".bus.PolylineActivity"/>

        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".activities.login.LoginActivity">
        </activity>

        <activity android:name=".activities.login.RegisterActivity">
        </activity>

        <activity android:name=".activities.MapActivity">
        </activity>

        <activity android:name=".activities.photoview.ShowImageActivity">
        </activity>

    </application>

</manifest>